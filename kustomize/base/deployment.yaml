apiVersion: apps/v1
kind: Deployment
metadata:
  name: pgerator
  labels:
    app.kubernetes.io/name: fint-pgerator
    app.kubernetes.io/instance: pgerator
    app.kubernetes.io/version: "latest"
    app.kubernetes.io/managed-by: GA
    app.kubernetes.io/component: operator
    app.kubernetes.io/part-of: fintlabs-application-infrastructure-services
    fintlabs.no/team: flais
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: fint-pgerator
      app.kubernetes.io/instance: pgerator
  template:
    metadata:
      annotations:
        prometheus.io/path: /actuator/prometheus
        prometheus.io/port: "8080"
        prometheus.io/scrape: "true"
      labels:
        app.kubernetes.io/name: fint-pgerator
        app.kubernetes.io/instance: pgerator
    spec:
      serviceAccountName: pgerator
      securityContext:
        {}
      containers:
        - name: pgerator
          securityContext:
            {}
          image: "ghcr.io/fintlabs/fint-pgerator:sha-dde1811"
          imagePullPolicy: IfNotPresent
          env:
            - name: logging.level.no.fintlabs
              value: DEBUG
          envFrom:
            - secretRef:
                name: pgerator
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          # livenessProbe:
          #   httpGet:
          #     path: /actuator/health
          #     port: http
          # readinessProbe:
          #   httpGet:
          #     path: /
          #     port: http
          resources:
            limits:
              memory: 512Mi
              cpu: 500m
            requests:
              memory: 256Mi
              cpu: 250m