spring:
  application:
    name: pgerator


logging:
  level:
    no.fintlabs: debug

fint:
  aiven:
    token:
    base-url: https://api.aiven.io/v1
    project: fintlabs
    service:
    pg:
      connection-pool-size: 2

management:
  endpoints:
    web:
      exposure:
        include: "*"